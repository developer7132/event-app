
export const state = {
  events: []
};
export const mutations = {
  
  
};
export const getters = {
  events: ({ events }) => events  
};
export const actions = {
  
  
};

export default {
  namespaced: true,
  state,
  mutations,
  getters,
  actions,
};
